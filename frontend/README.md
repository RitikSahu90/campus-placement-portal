# Campus Placement Portal Frontend

React + TypeScript + Vite frontend for the Campus Placement Portal.

```bash
npm install
npm run dev
```

The app expects the backend at `http://localhost:5000/api`. Override it with:

```bash
VITE_API_URL=http://localhost:5000/api npm run dev
```
